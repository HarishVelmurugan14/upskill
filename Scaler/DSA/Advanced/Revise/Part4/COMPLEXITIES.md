# Complexities — Part 4

> Only complexities from source `// Complexity` comments. Empty brackets `[  ]` → "not stated".

## d44_Heaps_Introduction

> All complexity stubs are empty — not stated.

## d45_Heaps_SortAndGreedy

> All complexity stubs are empty — not stated.

## d46_Heaps_LabSessions

> All complexity stubs are empty — not stated.

## d48_Heaps_InterviewProblems

> All complexity stubs are empty — not stated.

## d49_DP_OneDimensional

> All complexity stubs are empty — not stated.

## d50_DP_TwoDimensional

| Method | Time | Space |
|---|---|---|
| `numBSTrees` (Catalan) | O(A²) | (from comment: "#states × work per state = A × A iterations") |
| `nDigitNumbers` | O(A×B) | not stated |
| `maxSum` | O(N) | not stated |

## d52_DP_KnapsackApplications

| Method | Time | Space |
|---|---|---|
| `maxProfitFromRodCutting` | O(N²) | — |

## d53_Graphs_Introduction

| Method | Time | Space |
|---|---|---|
| `buildAdjacencyListForDirectedGraph` | O(V) | O(V) |
| `cycleDetectionInAGraph` | O(V+E) | O(V+E) |

> Also noted in source: "Time Complexity: O(A + M) — for building the adjacency list and DFS traversal. M : Number of edges"
> "Space Complexity: O(A + M) — for the adjacency list, visited arrays, and recursion stack."

## d54–d56

> All complexity stubs are empty — not stated.

> **Note**: Most Part4 source files have empty `// Complexity` comments. Only d50, d52, and d53 have stated values.
